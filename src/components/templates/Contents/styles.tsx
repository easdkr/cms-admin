import { Box } from "@mui/material";
import { styled } from "@mui/system";

export const ContentsLayout = styled(Box)({
    padding: 1,
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap"
})