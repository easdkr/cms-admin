import { SxProps, Theme } from '@mui/material'
import Colors from 'utils/styles/colors'
import Size from 'utils/styles/size'

export const ContentEditorLayoutStyles = {
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'space-between',
  height: '75%',
} as SxProps
